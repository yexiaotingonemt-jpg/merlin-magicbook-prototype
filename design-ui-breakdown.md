# 正式 UI 组件拆解

本文件记录探索事件卡与战斗魔法书的视觉组件拆解。此次仅替换视觉资产与界面结构，不改变探索、战斗、事件或卡牌规则。

## 探索事件卡

- 卡片结构：`场景插画 / 事件名称与说明 / 决策信息 / 操作提示`。
- 插画比例：3:2；桌面单屏模式约 248px 高，使用 `cover` 保持主体居中。
- 场景资产：怪物试炼、残破书库、元素池、休息室、转化实验室、幽灵导师、整理装订、魔法课堂、镜像法师各自使用独立插画。
- 信息覆盖：倒计时位于插画右上角；事件图标位于插画左下角，保留可识别性但不遮挡主体。
- 视觉层级：场景画面承担叙事；标题和收益、代价、战斗、警示、时限承担决策；不再用纯色纹理代替场景。

## 战斗开卷魔法书

- 外层结构：中央区域是一张完整开卷书本资产，不再是两个并列卡片。
- 内容结构：现有我方与敌方动态咒语内容分别嵌入左、右书页的安全区域。
- 安全区域：左右各约占书本宽度 39%，中间保留约 5% 书脊空间，外侧保留页厚和封皮。
- 内容滚动：咒语效果超出单页时只滚动页内内容，不改变书本外形。
- 动画：翻页动画以左右书脊为轴；尊重 `prefers-reduced-motion`。
- 自适应：桌面保持完整开卷；窄屏改为纵向两页，但仍保留同一书本背景和页缘材质。

## 素材规范

- 场景插画：960×640 WebP，质量 84。
- 开卷书本：1536×1024 WebP，质量 88。
- 所有素材不内嵌 UI 文案，确保事件、咒语和数值仍由 DOM 动态渲染。
